# Docker Compose for Python Ingest Service
# Parcel State Tracker Platform
#
# Note: MongoDB is deployed separately via the data-bases service

services:
  # Python Ingest Service
  service-ingest-python:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parcel-tracker-ingest-python
    restart: unless-stopped
    environment:
      MONGODB_HOST: ${MONGODB_HOST:-localhost}
      MONGODB_PORT: ${MONGODB_PORT:-27017}
      MONGODB_USERNAME: ${MONGODB_USERNAME:-admin}
      MONGODB_PASSWORD: ${MONGODB_PASSWORD:-admin123}
      MONGODB_DB_NAME: ${MONGODB_DB_NAME:-parcel_tracker}
      MONGODB_AUTH_SOURCE: admin
      FLASK_ENV: ${FLASK_ENV:-production}
      FLASK_PORT: 5000
    ports:
      - "${FLASK_PORT:-5000}:5000"
    networks:
      - parcel-tracker-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# Custom network for service communication
networks:
  parcel-tracker-network:
    driver: bridge
